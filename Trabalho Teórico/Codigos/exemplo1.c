#include <stdio.h>
#include <stdlib.h>

int main()
{
    char velocity = '4';

    // Print como char
    printf("velocity = %c\n", velocity);

    // Print como int
    printf("velocity = %d\n", velocity - '0');

    return 0;
}